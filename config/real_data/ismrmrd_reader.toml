[ismrmrd_reader]
# Print one line per raw parallel-calibration acquisition (acquisition, ky, z, repetition).
# This affects logging only; it does not change which raw data are read or retained.
print_raw_calibration_lines = false
